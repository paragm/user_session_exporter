---
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.23
    steps:
      - checkout
      - run:
          name: Build
          command: make build
      - run:
          name: Test
          command: make test

workflows:
  version: 2
  default:
    jobs:
      - build
